# Linux shell scripts must always use LF line endings
*.sh text eol=lf

# Docker-related files should also use LF
Dockerfile text eol=lf

# Docker Compose / YAML configuration
*.yml text eol=lf

*.yaml text eol=lf
